import { DefaultApiClient } from 'ask-sdk';
import { services } from 'ask-sdk-model';
import { ApiClient } from './apiClient';
export declare class V1ApiClient implements ApiClient {
    protected defaultApiClient: DefaultApiClient;
    constructor();
    post(uri: string, headers: Array<{
        key: string;
        value: string;
    }>, body: string): Promise<services.ApiClientResponse>;
    put(uri: string, headers: Array<{
        key: string;
        value: string;
    }>, body: string): Promise<services.ApiClientResponse>;
    get(uri: string, headers: Array<{
        key: string;
        value: string;
    }>): Promise<services.ApiClientResponse>;
    delete(uri: string, headers: Array<{
        key: string;
        value: string;
    }>): Promise<services.ApiClientResponse>;
    private buildOptions;
    private dispatch;
}

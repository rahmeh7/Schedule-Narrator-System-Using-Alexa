import { VoicePlayerSpeakDirective } from '../directives/voicePlayerSpeakDirective';
import { ApiClient } from './apiClient';
export declare class DirectiveService {
    protected apiClient: ApiClient;
    protected directivesApiPath: string;
    constructor(apiClient?: ApiClient);
    enqueue(directive: VoicePlayerSpeakDirective, apiEndpoint: string, token: string): Promise<void>;
    private dispatch;
    private validateApiResponse;
}

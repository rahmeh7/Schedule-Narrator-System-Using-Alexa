import { services } from 'ask-sdk-model';
export declare class VoicePlayerSpeakDirective implements services.directive.SendDirectiveRequest {
    directive: services.directive.Directive;
    header: services.directive.Header;
    constructor(id: string, speechContent: string);
}

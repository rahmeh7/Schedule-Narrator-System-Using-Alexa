import { interfaces } from 'ask-sdk-model';
export declare class TextUtils {
    static makePlainText(text: string): interfaces.display.PlainText;
    static makeRichText(text: string): interfaces.display.RichText;
    static makeTextContent(primaryText: interfaces.display.TextField, secondaryText: interfaces.display.TextField, tertiaryText: interfaces.display.TextField): interfaces.display.TextContent;
}

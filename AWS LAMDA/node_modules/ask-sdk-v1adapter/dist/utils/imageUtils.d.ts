import { interfaces } from 'ask-sdk-model';
export declare class ImageUtils {
    static makeImage(url: string, widthPixels?: number, heightPixels?: number, size?: interfaces.display.ImageSize, description?: string): interfaces.display.Image;
    static makeImages(imgArr: interfaces.display.ImageInstance[], description?: string): interfaces.display.Image;
}
